Tests

* If virtual environment is activated, then pass

* If pip freeze shows all of the requirements, then pass
requirements: flask, sqlalchemy, flask-sqlalchemy, psycopg2, flask-wtf

* If 'Hello World' displays on the browser, then pass

* If your database shows up on SQl Workbench, then passes

* If tables are created in the SQL workbench, then pass

* If one data point displays in the SQL workbench, then pass


